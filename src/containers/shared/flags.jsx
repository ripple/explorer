/*
These are the supported flags for each api call. Please add them as necessary.
 */

const flags = {
  accountInfo: {
    // If the account root is an AMM
    lsfAMM: 0x02000000,
  },
}

export default flags
